# @0x1461a0.cc/site

Personal blog site.

## Development

```bash
pnpm dev
```

## Build

```bash
pnpm build
```

The `prebuild` script clones content from a remote repo. Requires the `CONTENT_REPO_URL` environment variable.
